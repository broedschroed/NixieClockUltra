# IR-Fernbedienung + Power-Save-Toggle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** VS1838-IR-Empfänger auf Pin 48 einbinden, 7 Fernbedienungsfunktionen über Web-UI anlernbar machen und Power-Save per IR + Web-Toggle schaltbar machen.

**Architecture:** Alle Änderungen erfolgen in der einzelnen `NixieClockUltra.ino`. Die `IRremoteESP8266`-Bibliothek empfängt Codes per GPIO-Interrupt (nicht-blockierend). Im `loop()` wird per `irrecv.decode()` gepollt. `strip.show()` wird von `irrecv.pause()/resume()` eingeschlossen, um Timing-Konflikte zu vermeiden. Gelernte Codes werden als `uint64_t` in NVS (Preferences) persistiert.

**Tech Stack:** ESP32-S3, Arduino Core 3.x, IRremoteESP8266 (Library Manager), Adafruit NeoPixel, ESPAsyncWebServer (ESP32Async-Fork), ArduinoJson 6.x

> **Hinweis zu TDD:** Da kein Unit-Test-Framework für embedded Arduino existiert, ist der Verifikationsschritt pro Task das erfolgreiche Kompilieren (`Sketch → Verify`) sowie – sobald Hardware verfügbar – das Beobachten des Serial-Monitor-Outputs.

---

## Dateistruktur

Nur eine Datei wird modifiziert:

- **Modify:** `NixieClockUltra.ino`
  - Neuer Block: IR-Includes + Pin-Define (nach bestehenden Includes)
  - Neuer Block: IR-Globals (Enum, Array, Lernzustand) + `powerSaveEnabled`
  - Modifiziert: `handlePowerSave()` – prüft `powerSaveEnabled`
  - Modifiziert: `updateNeoPixel()` – `strip.show()` eingeschlossen
  - Modifiziert: `setup()` – IR-Codes laden, `irrecv.enableIRIn()`
  - Modifiziert: `loop()` – `handleIR()` aufrufen
  - Neue Funktion: `handleIR()`
  - Neue Funktion: `dispatchIRAction()`
  - Neue Funktion: `executeAction()`
  - Modifiziert: `setupWebServer()` – 4 neue Endpunkte
  - Modifiziert: `WEB_PAGE` PROGMEM-String – Power-Save-Toggle + IR-Karte

---

## Task 1: IRremoteESP8266 installieren und Includes + Pin-Define hinzufügen

**Files:**
- Modify: `NixieClockUltra.ino` (INCLUDES-Block, Zeile ~29)

- [ ] **Schritt 1: Bibliothek installieren**

  Arduino IDE → Sketch → Bibliotheken verwalten → Suche: `IRremoteESP8266` → Install (crankyoldgit).
  Sicherstellen dass Version ≥ 2.8.x installiert ist.

- [ ] **Schritt 2: Includes einfügen**

  Nach `#include <Preferences.h>` folgende Zeilen einfügen:

  ```cpp
  #include <IRremoteESP8266.h>
  #include <IRrecv.h>
  ```

- [ ] **Schritt 3: Pin-Define einfügen**

  Nach `#define NEO_COUNT 10` einfügen:

  ```cpp
  // IR-Empfänger
  #define IR_RECV_PIN  48
  ```

- [ ] **Schritt 4: Kompilieren**

  Arduino IDE → Sketch → Verify/Compile. Erwartet: Kein Fehler.

---

## Task 2: IR-Globals und powerSaveEnabled deklarieren

**Files:**
- Modify: `NixieClockUltra.ino` (GLOBALE VARIABLEN-Block, nach `hw_timer_t *muxTimer`)

- [ ] **Schritt 1: Enum und Hilfstabellen einfügen**

  Direkt vor dem `// TASTER-STRUKTUR`-Kommentar einfügen:

  ```cpp
  // ═══════════════════════════════════════════════════════════
  //  IR-FERNBEDIENUNG
  // ═══════════════════════════════════════════════════════════
  enum IrAction {
    IR_LEARN_NONE           = -1,
    IR_ACTION_SET           = 0,
    IR_ACTION_UP            = 1,
    IR_ACTION_DOWN          = 2,
    IR_ACTION_BRIGHTNESS    = 3,
    IR_ACTION_ANIM_NEXT     = 4,
    IR_ACTION_SLOT          = 5,
    IR_ACTION_POWER_SAVE_TOGGLE = 6,
    IR_ACTION_COUNT         = 7
  };

  const char* IR_ACTION_KEYS[IR_ACTION_COUNT] = {
    "ir_SET", "ir_UP", "ir_DOWN", "ir_BRIGHTNESS",
    "ir_ANIM_NEXT", "ir_SLOT", "ir_PSTOGGLE"
  };

  const char* IR_ACTION_LABELS[IR_ACTION_COUNT] = {
    "SET", "UP", "DOWN", "BRIGHTNESS",
    "ANIM_NEXT", "SLOT", "POWER_SAVE_TOGGLE"
  };

  IRrecv   irrecv(IR_RECV_PIN);
  decode_results irResults;

  uint64_t irCodes[IR_ACTION_COUNT] = {0};
  IrAction irLearnTarget  = IR_LEARN_NONE;
  unsigned long irLearnStartMs = 0;
  #define IR_LEARN_TIMEOUT_MS 10000

  // Power-Save dauerhaft aktiviert/deaktiviert
  bool powerSaveEnabled = true;
  ```

- [ ] **Schritt 2: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 3: handlePowerSave() anpassen

**Files:**
- Modify: `NixieClockUltra.ino` (Funktion `handlePowerSave()`, ca. Zeile 501)

- [ ] **Schritt 1: Funktion ersetzen**

  Die gesamte Funktion `handlePowerSave()` ersetzen durch:

  ```cpp
  void handlePowerSave() {
    if (!powerSaveEnabled) {
      if (powerSaveActive) powerSaveActive = false;
      return;
    }
    unsigned long idleSec = (millis() - lastInteractionMs) / 1000;
    if (idleSec >= POWER_SAVE_TIMEOUT_S && !powerSaveActive) {
      powerSaveActive = true;
    } else if (idleSec < POWER_SAVE_TIMEOUT_S && powerSaveActive) {
      powerSaveActive = false;
    }
  }
  ```

- [ ] **Schritt 2: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 4: strip.show() mit IR-Pause einschließen

**Files:**
- Modify: `NixieClockUltra.ino` (Funktion `updateNeoPixel()`, letzter Aufruf `strip.show()`)

- [ ] **Schritt 1: strip.show() wrappen**

  In `updateNeoPixel()` ganz am Ende, die einzelne Zeile:

  ```cpp
    strip.show();
  ```

  ersetzen durch:

  ```cpp
    irrecv.pause();
    strip.show();
    irrecv.resume();
  ```

- [ ] **Schritt 2: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 5: IR-Handler-Funktionen hinzufügen

**Files:**
- Modify: `NixieClockUltra.ino` (neuer Block nach `handlePowerSave()`, vor `setupWifi()`)

- [ ] **Schritt 1: executeAction() einfügen**

  Nach `handlePowerSave()` einfügen:

  ```cpp
  // ═══════════════════════════════════════════════════════════
  //  IR-AKTIONEN
  // ═══════════════════════════════════════════════════════════
  void executeAction(IrAction action) {
    switch (action) {
      case IR_ACTION_SET:
        if (editState == EDIT_NONE) {
          editState = EDIT_HOUR;
          editEnterTime = millis();
        } else if (editState == EDIT_HOUR) {
          editState = EDIT_MIN;
          editEnterTime = millis();
        } else if (editState == EDIT_MIN) {
          editState = EDIT_SEC;
          editEnterTime = millis();
        } else {
          writeRTC();
          editState = EDIT_NONE;
        }
        break;

      case IR_ACTION_UP:
        if (editState == EDIT_HOUR) { curHour = (curHour + 1) % 24; editEnterTime = millis(); }
        else if (editState == EDIT_MIN) { curMin = (curMin + 1) % 60; editEnterTime = millis(); }
        else if (editState == EDIT_SEC) { curSec = (curSec + 1) % 60; editEnterTime = millis(); }
        break;

      case IR_ACTION_DOWN:
        if (editState == EDIT_HOUR) { curHour = (curHour + 23) % 24; editEnterTime = millis(); }
        else if (editState == EDIT_MIN) { curMin = (curMin + 59) % 60; editEnterTime = millis(); }
        else if (editState == EDIT_SEC) { curSec = (curSec + 59) % 60; editEnterTime = millis(); }
        break;

      case IR_ACTION_BRIGHTNESS:
        brightLevel = (brightLevel + 1) % 4;
        neoBright   = BRIGHTNESS_LEVELS[brightLevel];
        prefs.putUChar("bright", brightLevel);
        prefs.putUChar("neoBright", neoBright);
        break;

      case IR_ACTION_ANIM_NEXT:
        animMode = (AnimMode)((int(animMode) + 1) % ANIM_COUNT);
        prefs.putUChar("animMode", (uint8_t)animMode);
        break;

      case IR_ACTION_SLOT:
        startSlotAnimation(curHour, curMin, curSec);
        break;

      case IR_ACTION_POWER_SAVE_TOGGLE:
        powerSaveEnabled = !powerSaveEnabled;
        prefs.putBool("psEnabled", powerSaveEnabled);
        if (!powerSaveEnabled) powerSaveActive = false;
        break;

      default: break;
    }
  }

  void dispatchIRAction(uint64_t code) {
    for (int i = 0; i < IR_ACTION_COUNT; i++) {
      if (irCodes[i] != 0 && irCodes[i] == code) {
        executeAction((IrAction)i);
        lastInteractionMs = millis();
        return;
      }
    }
  }

  void handleIR() {
    // Lernmodus-Timeout
    if (irLearnTarget != IR_LEARN_NONE &&
        millis() - irLearnStartMs > IR_LEARN_TIMEOUT_MS) {
      irLearnTarget = IR_LEARN_NONE;
    }

    if (!irrecv.decode(&irResults)) return;

    uint64_t code = irResults.value;
    irrecv.resume();

    // Repeat-Codes ignorieren (NEC sendet 0xFFFFFFFF als Repeat)
    if (code == 0xFFFFFFFFULL || code == 0xFFFFFFFFFFFFFFFFULL) return;

    if (irLearnTarget != IR_LEARN_NONE) {
      irCodes[(int)irLearnTarget] = code;
      prefs.putULong64(IR_ACTION_KEYS[(int)irLearnTarget], code);
      Serial.printf("[IR] Gelernt: %s = 0x%08X\n",
        IR_ACTION_LABELS[(int)irLearnTarget], (uint32_t)code);
      irLearnTarget = IR_LEARN_NONE;
    } else {
      dispatchIRAction(code);
    }
  }
  ```

- [ ] **Schritt 2: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 6: setup() und loop() anpassen

**Files:**
- Modify: `NixieClockUltra.ino` (Funktionen `setup()` und `loop()`)

- [ ] **Schritt 1: IR-Codes und powerSaveEnabled in setup() laden**

  Im `setup()`-Block, direkt nach den drei `prefs.getUChar(...)`-Zeilen (ca. Zeile 923–925) einfügen:

  ```cpp
  for (int i = 0; i < IR_ACTION_COUNT; i++) {
    irCodes[i] = prefs.getULong64(IR_ACTION_KEYS[i], 0);
  }
  powerSaveEnabled = prefs.getBool("psEnabled", true);
  ```

- [ ] **Schritt 2: irrecv.enableIRIn() in setup() starten**

  Im `setup()`-Block direkt nach `setupWebServer()` einfügen:

  ```cpp
  // --- IR-Empfänger ---
  irrecv.enableIRIn();
  Serial.println("[IR] Empfänger gestartet auf Pin " + String(IR_RECV_PIN));
  ```

- [ ] **Schritt 3: handleIR() im loop() aufrufen**

  Im `loop()`-Block, direkt nach `updateButton(btnLight);` (ca. Zeile 985) einfügen:

  ```cpp
  // --- IR ---
  handleIR();
  ```

- [ ] **Schritt 4: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 7: Web-API-Endpunkte hinzufügen

**Files:**
- Modify: `NixieClockUltra.ino` (Funktion `setupWebServer()`, nach dem `/api/slot`-Handler)

- [ ] **Schritt 1: /api/status um psEnabled erweitern**

  Im Handler für `/api/status` (ca. Zeile 748), nach `doc["slot"] = slotActive;` einfügen:

  ```cpp
  doc["psEnabled"] = powerSaveEnabled;
  ```

  Außerdem die `StaticJsonDocument<200>`-Größe auf `<256>` erhöhen, da ein Feld dazukommt:

  ```cpp
  StaticJsonDocument<256> doc;
  ```

- [ ] **Schritt 2: Vier neue Endpunkte einfügen**

  Im `setupWebServer()`-Block, direkt vor `server.begin();` einfügen:

  ```cpp
  // --- IR: Status ---
  server.on("/api/ir/status", HTTP_GET, [](AsyncWebServerRequest *req) {
    StaticJsonDocument<512> doc;
    if (irLearnTarget != IR_LEARN_NONE) {
      doc["learning"] = IR_ACTION_LABELS[(int)irLearnTarget];
    } else {
      doc["learning"] = "";
    }
    JsonObject codes = doc.createNestedObject("codes");
    for (int i = 0; i < IR_ACTION_COUNT; i++) {
      if (irCodes[i] != 0) {
        char buf[12];
        snprintf(buf, sizeof(buf), "0x%08X", (uint32_t)irCodes[i]);
        codes[IR_ACTION_LABELS[i]] = buf;
      } else {
        codes[IR_ACTION_LABELS[i]] = "";
      }
    }
    String out; serializeJson(doc, out);
    req->send(200, "application/json", out);
  });

  // --- IR: Anlernen starten ---
  server.on("/api/ir/learn", HTTP_POST, [](AsyncWebServerRequest *req){},
    nullptr,
    [](AsyncWebServerRequest *req, uint8_t *data, size_t len, size_t, size_t) {
      StaticJsonDocument<64> doc;
      if (!deserializeJson(doc, data, len)) {
        String action = doc["action"].as<String>();
        for (int i = 0; i < IR_ACTION_COUNT; i++) {
          if (action == IR_ACTION_LABELS[i]) {
            irLearnTarget  = (IrAction)i;
            irLearnStartMs = millis();
            req->send(200, "application/json", "{\"ok\":true}");
            return;
          }
        }
      }
      req->send(400, "application/json", "{\"ok\":false}");
    }
  );

  // --- IR: Code löschen ---
  server.on("/api/ir/clear", HTTP_POST, [](AsyncWebServerRequest *req){},
    nullptr,
    [](AsyncWebServerRequest *req, uint8_t *data, size_t len, size_t, size_t) {
      StaticJsonDocument<64> doc;
      if (!deserializeJson(doc, data, len)) {
        String action = doc["action"].as<String>();
        for (int i = 0; i < IR_ACTION_COUNT; i++) {
          if (action == IR_ACTION_LABELS[i]) {
            irCodes[i] = 0;
            prefs.putULong64(IR_ACTION_KEYS[i], 0);
            req->send(200, "application/json", "{\"ok\":true}");
            return;
          }
        }
      }
      req->send(400, "application/json", "{\"ok\":false}");
    }
  );

  // --- Power-Save Toggle ---
  server.on("/api/powersave", HTTP_POST, [](AsyncWebServerRequest *req){},
    nullptr,
    [](AsyncWebServerRequest *req, uint8_t *data, size_t len, size_t, size_t) {
      StaticJsonDocument<64> doc;
      if (!deserializeJson(doc, data, len)) {
        powerSaveEnabled = (bool)doc["enabled"];
        prefs.putBool("psEnabled", powerSaveEnabled);
        if (!powerSaveEnabled) powerSaveActive = false;
      }
      req->send(200, "application/json", "{\"ok\":true}");
    }
  );
  ```

- [ ] **Schritt 3: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 8: Web-UI aktualisieren (Power-Save-Toggle + IR-Karte)

**Files:**
- Modify: `NixieClockUltra.ino` (PROGMEM-String `WEB_PAGE`)

- [ ] **Schritt 1: CSS für Toggle und Blink-Animation ergänzen**

  Im `<style>`-Block des `WEB_PAGE`-Strings, direkt vor `</style>` einfügen:

  ```css
  @keyframes blink{0%,100%{background:var(--card)}50%{background:#3a2800}}
  .toggle{position:relative;display:inline-block;width:42px;height:24px}
  .toggle input{opacity:0;width:0;height:0}
  .slider{position:absolute;cursor:pointer;top:0;left:0;right:0;bottom:0;background:#333;border-radius:24px;transition:.3s}
  .slider:before{content:'';position:absolute;height:18px;width:18px;left:3px;bottom:3px;background:#666;border-radius:50%;transition:.3s}
  .toggle input:checked+.slider{background:var(--accent)}
  .toggle input:checked+.slider:before{transform:translateX(18px);background:#000}
  ```

- [ ] **Schritt 2: Power-Save-Toggle in Helligkeit-Karte einfügen**

  In der Helligkeit-Karte (`<h2>💡 Helligkeit &amp; Animation</h2>`), direkt vor dem schließenden `</div>` der Karte einfügen:

  ```html
  <div class="row">
    <label>Power-Save (Auto-Dimmen)</label>
    <label class="toggle"><input type="checkbox" id="psEnabled" onchange="setPowerSave(this.checked)"><span class="slider"></span></label>
  </div>
  ```

- [ ] **Schritt 3: IR-Karte einfügen**

  Nach der Slot-Machine-Karte (`</div>` nach `<h2>🎰 Slot-Animation`) und vor der WLAN-Karte einfügen:

  ```html
  <div class="card">
    <h2>📡 IR-Fernbedienung</h2>
    <table style="width:100%;border-collapse:collapse">
      <thead><tr>
        <th style="text-align:left;color:var(--dim);font-size:.8em;padding:4px 0;width:45%">Funktion</th>
        <th style="text-align:left;color:var(--dim);font-size:.8em;padding:4px 0">Code</th>
        <th></th>
      </tr></thead>
      <tbody id="irRows"></tbody>
    </table>
    <p id="irMsg" style="font-size:.75em;color:var(--dim);margin-top:8px;min-height:1.2em"></p>
  </div>
  ```

- [ ] **Schritt 4: JavaScript einfügen**

  Im `<script>`-Block, direkt vor `setInterval(refreshClock,1000);` einfügen:

  ```js
  const IR_ACTIONS=['SET','UP','DOWN','BRIGHTNESS','ANIM_NEXT','SLOT','POWER_SAVE_TOGGLE'];
  const IR_LABELS={'SET':'SET – Einstellmodus','UP':'UP – Erhöhen','DOWN':'DOWN – Verringern',
    'BRIGHTNESS':'BRIGHTNESS – Helligkeit','ANIM_NEXT':'ANIM – nächste Animation',
    'SLOT':'SLOT – Slot-Maschine','POWER_SAVE_TOGGLE':'POWER SAVE – Toggle'};
  let irPollTimer=null;

  async function refreshIR(){
    let d=await get('/api/ir/status');
    if(!d.codes)return;
    let tbody=document.getElementById('irRows');
    tbody.innerHTML='';
    IR_ACTIONS.forEach(a=>{
      let code=d.codes[a]||'';
      let learning=(d.learning===a);
      let tr=document.createElement('tr');
      tr.id='irr-'+a;
      if(learning)tr.style.animation='blink 0.6s infinite';
      tr.innerHTML=`<td style="padding:4px 8px 4px 0;font-size:.82em">${IR_LABELS[a]}</td>`+
        `<td style="padding:4px 8px;font-size:.8em;color:${code?'var(--accent)':'var(--dim)'}">${code||'—'}</td>`+
        `<td style="white-space:nowrap">`+
          `<button class="sec" style="padding:3px 9px;font-size:.78em" onclick="irLearn('${a}')">${learning?'warte…':'Anlernen'}</button> `+
          `<button class="sec" style="padding:3px 8px;font-size:.78em" onclick="irClear('${a}')">✕</button>`+
        `</td>`;
      tbody.appendChild(tr);
    });
    if(d.learning){
      document.getElementById('irMsg').textContent='Taste auf Fernbedienung drücken… (10 s Timeout)';
      if(!irPollTimer)irPollTimer=setInterval(async()=>{let s=await get('/api/ir/status');refreshIR();if(!s.learning){clearInterval(irPollTimer);irPollTimer=null;}},500);
    }else{
      document.getElementById('irMsg').textContent='';
      if(irPollTimer){clearInterval(irPollTimer);irPollTimer=null;}
    }
  }

  async function irLearn(action){
    await api('/api/ir/learn',{action});
    refreshIR();
  }
  async function irClear(action){
    await api('/api/ir/clear',{action});
    refreshIR();
  }
  async function setPowerSave(enabled){
    await api('/api/powersave',{enabled});
  }
  ```

- [ ] **Schritt 5: refreshClock() um psEnabled und IR-Initialisierung erweitern**

  In der Funktion `refreshClock()`, nach der letzten `if(d.anim...)`-Zeile einfügen:

  ```js
  if(d.psEnabled!==undefined)document.getElementById('psEnabled').checked=d.psEnabled;
  ```

  Ganz am Ende des `<script>`-Blocks, nach `refreshWifi();` anfügen:

  ```js
  refreshIR();
  ```

- [ ] **Schritt 6: Kompilieren**

  Sketch → Verify. Erwartet: Kein Fehler.

---

## Task 9: Integrationstest + Commit

**Files:**
- Modify: `NixieClockUltra.ino` (keine Codeänderung, nur Verifikation)

- [ ] **Schritt 1: Vollständigen Build durchführen**

  Sketch → Verify/Compile (oder Upload auf DevBoard falls verfügbar).
  Erwartet: 0 Fehler, 0 Warnungen (außer ggf. „unused variable" von der Library selbst).

- [ ] **Schritt 2: Serial-Monitor-Check (wenn Hardware verfügbar)**

  Nach Upload Serial-Monitor öffnen (115200 Baud). Erwartet bei Boot:
  ```
  [IR] Empfänger gestartet auf Pin 48
  [NixieClock] Bereit.
  ```

  Beliebige Fernbedienungstaste drücken → Erwartet:
  ```
  [IR] Gelernt: SET = 0x....  (nach Anlernen)
  ```

- [ ] **Schritt 3: Web-UI prüfen (wenn Hardware verfügbar)**

  Mit `NixieClock`-WLAN verbinden → `http://192.168.4.1` aufrufen.
  Checkliste:
  - [ ] Power-Save-Toggle sichtbar in Helligkeit-Karte, Zustand korrekt
  - [ ] IR-Karte mit 7 Zeilen sichtbar, alle zeigen `—`
  - [ ] [Anlernen] klicken → Zeile blinkt orange, Status-Text erscheint
  - [ ] Fernbedienung drücken → Code erscheint, Blinken stoppt
  - [ ] [✕] löscht den Code
  - [ ] Power-Save-Toggle umschalten → gespeicherter Zustand bleibt nach Neustart

- [ ] **Schritt 4: Commit**

  ```bash
  git add NixieClockUltra.ino docs/superpowers/specs/2026-04-22-ir-remote-design.md docs/superpowers/plans/2026-04-22-ir-remote.md
  git commit -m "feat: IR remote control with web-based learning mode and power-save toggle

  - VS1838 on pin 48 via IRremoteESP8266
  - 7 learnable functions: SET/UP/DOWN/BRIGHTNESS/ANIM_NEXT/SLOT/POWER_SAVE_TOGGLE
  - IR codes persisted in NVS (Preferences)
  - strip.show() wrapped with irrecv.pause()/resume() to prevent timing conflicts
  - Power-save can be permanently disabled via IR or web toggle
  - New web card with per-function learn/clear UI, 500ms polling during learn mode"
  ```

---

## Self-Review

**Spec-Abdeckung:**
- [x] VS1838 Pin 48 → `IR_RECV_PIN = 48`, Task 1
- [x] IRremoteESP8266 → Task 1
- [x] 7 Funktionen (inkl. POWER_SAVE_TOGGLE) → Task 2, Task 5
- [x] NeoPixel-Konflikt pause/resume → Task 4
- [x] NVS-Persistenz (uint64_t + psEnabled) → Task 2, Task 6
- [x] Lernmodus mit 10-s-Timeout → Task 5 (handleIR)
- [x] Repeat-Code-Filter → Task 5 (handleIR)
- [x] /api/ir/status, /api/ir/learn, /api/ir/clear, /api/powersave → Task 7
- [x] /api/status um psEnabled erweitert → Task 7
- [x] Web-UI: Power-Save-Toggle → Task 8
- [x] Web-UI: IR-Karte mit Tabelle + Polling → Task 8
- [x] handlePowerSave() respektiert powerSaveEnabled → Task 3

**Typ-Konsistenz:**
- `IrAction` Enum → definiert Task 2, verwendet Task 5, Task 7
- `IR_ACTION_KEYS[]` → definiert Task 2, verwendet Task 5 (save/load), Task 7 (clear)
- `IR_ACTION_LABELS[]` → definiert Task 2, verwendet Task 7 (API), Task 8 (JS)
- `irLearnTarget` → Task 2 (deklariert), Task 5 (gesetzt), Task 7 (gelesen)
- `powerSaveEnabled` → Task 2 (deklariert), Task 3 (geprüft), Task 6 (gesetzt), Task 7 (API), Task 8 (UI)
